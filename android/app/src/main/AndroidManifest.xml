<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
              Pont de session Google natif (Custom Tabs → WebView) — voir
              MOBILE_SETUP.md et src/app/api/auth/mobile/*. Schéma custom, pas
              App Links : pas de keystore de signature release pour l'instant,
              donc pas de fingerprint stable pour assetlinks.json. Le risque
              (une autre app enregistrant le même schéma) est déjà couvert par
              le code lui-même (256 bits, usage unique, TTL 2 min) — migration
              vers App Links documentée dans mobileAuthCode.ts si un keystore
              release existe un jour.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="habitgame" android:host="auth-callback" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>

        <!--
          AdMob App ID — requis, l'app crashe au lancement sans ce meta-data.
          Valeur actuelle = ID de TEST officiel Google (jamais de vraies pubs
          servies). Remplacer par le vrai App ID Android une fois le compte
          AdMob créé et l'app soumise pour review — voir MOBILE_SETUP.md.
        -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
